# Publication and release

Publication is the customer edge. It is not the platform API.

```bash
fystash publication list --json
fystash release list --json
```

Do not treat a Preview Access Grant as a Publication. Direct backend access is denied (`DIRECT_BACKEND_ACCESS_DENIED`). Use the public Publication hostname the Plan named.
